# Database-Management-System-DBMS-Class-Assignment
STUDENT INFORMATION:
NAMES: IRAKOZE Manzi Chris
Student ID:20252sen130
lecture Name: Ivan.


Assignment:
RRA wants to develop a database system to manage taxpayers, tax registrations, declarations, assessments, payments, audits, penalties, refunds, objections, enforcement cases, properties, vehicles, businesses, tax officers, and revenue collection centers. You are required to analyze the information provided and design a relational database named: REVENUE_ADMINISTRATION_DB The database must contain the 20 tables listed below. For every table: 1. Create the table using an appropriate table name. 2. Define suitable data types for all columns. 3. Define the primary key. 4. Define all necessary foreign keys. 5. Analyze and establish the correct relationships between the tables. 6. Apply appropriate constraints such as: o PRIMARY KEY o FOREIGN KEY o NOT NULL o UNIQUE o CHECK o DEFAULT 7. Insert at least 10 realistic records into every table. 8. Ensure that foreign-key values correspond to existing primary-key values. 9. Use meaningful Revenue Administration data. 10. Draw an Entity Relationship Diagram showing all relationships. 






PART 3: QUERIES 

1.SELECT tp.taxpayer_tin, tp.taxpayer_name, tp.taxpayer_type, tt.tax_type_name, tt.filing_frequency, tc.centre_name, tc.district_name, SUM(td.declared_amount) AS total_declared_amount, SUM(ta.assessed_amount) AS total_assessed_amount, SUM(pt.total_payment_amount) AS total_payment_amount FROM taxpayer tp INNER JOIN tax_registration tr ON tp.taxpayer_id = tr.taxpayer_id INNER JOIN tax_type tt ON tr.tax_type_id = tt.tax_type_id INNER JOIN tax_centre tc ON tr.tax_centre_id = tc.tax_centre_id INNER JOIN tax_declaration td ON tr.registration_id = td.registration_id INNER JOIN tax_assessment ta ON td.declaration_id = ta.declaration_id INNER JOIN (SELECT assessment_id, SUM(payment_amount) AS total_payment_amount FROM tax_payment GROUP BY assessment_id) pt ON ta.assessment_id = pt.assessment_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, tp.taxpayer_type, tt.tax_type_name, tt.filing_frequency, tc.centre_name, tc.district_name HAVING SUM(ta.assessed_amount) > 1000000 ORDER BY regexp_replace(tp.taxpayer_name, '\D', '', 'g')::int;

2.SELECT tp.taxpayer_tin, tp.taxpayer_name, tp.registration_date AS taxpayer_registration_date, tt.tax_type_name, tr.registration_date AS tax_registration_date, tc.centre_name, COUNT(td.declaration_id) AS number_of_declarations, COALESCE(SUM(td.declared_amount), 0) AS total_declared_amount FROM taxpayer tp LEFT JOIN tax_registration tr ON tp.taxpayer_id = tr.taxpayer_id LEFT JOIN tax_type tt ON tr.tax_type_id = tt.tax_type_id LEFT JOIN tax_centre tc ON tr.tax_centre_id = tc.tax_centre_id LEFT JOIN tax_declaration td ON tr.registration_id = td.registration_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, tp.registration_date, tt.tax_type_name, tr.registration_date, tc.centre_name HAVING COUNT(td.declaration_id) < 3;


3.SELECT tt.tax_type_id, tt.tax_type_name, tt.filing_frequency, COUNT(DISTINCT tr.taxpayer_id) AS number_of_registered_taxpayers, COALESCE(SUM(td.declared_amount), 0) AS total_declared_amount, COALESCE(SUM(ta.assessed_amount), 0) AS total_assessed_amount FROM tax_registration tr RIGHT JOIN tax_type tt ON tr.tax_type_id = tt.tax_type_id LEFT JOIN tax_declaration td ON tr.registration_id = td.registration_id LEFT JOIN tax_assessment ta ON td.declaration_id = ta.declaration_id GROUP BY tt.tax_type_id, tt.tax_type_name, tt.filing_frequency HAVING COALESCE(SUM(td.declared_amount), 0) < 5000000;

4.SELECT tp.taxpayer_tin, tp.taxpayer_name, b.business_name, b.business_sector, tt.tax_type_name, tc.centre_name, SUM(td.declared_amount) AS total_declared_amount, SUM(ta.assessed_amount) AS total_assessed_amount, SUM(COALESCE(pt.total_payment_amount, 0)) AS total_payment_amount, SUM(COALESCE(pn.total_penalty_amount, 0)) AS total_penalty_amount FROM taxpayer tp INNER JOIN business b ON tp.taxpayer_id = b.taxpayer_id INNER JOIN tax_registration tr ON tp.taxpayer_id = tr.taxpayer_id INNER JOIN tax_type tt ON tr.tax_type_id = tt.tax_type_id INNER JOIN tax_centre tc ON tr.tax_centre_id = tc.tax_centre_id INNER JOIN tax_declaration td ON tr.registration_id = td.registration_id INNER JOIN tax_assessment ta ON td.declaration_id = ta.declaration_id LEFT JOIN (SELECT assessment_id, SUM(payment_amount) AS total_payment_amount FROM tax_payment GROUP BY assessment_id) pt ON ta.assessment_id = pt.assessment_id LEFT JOIN (SELECT assessment_id, SUM(penalty_amount) AS total_penalty_amount FROM penalty GROUP BY assessment_id) pn ON ta.assessment_id = pn.assessment_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, b.business_name, b.business_sector, tt.tax_type_name, tc.centre_name HAVING SUM(ta.assessed_amount) > 10000000;

5.SELECT tp.taxpayer_tin, tp.taxpayer_name, pr.property_location, pr.property_value, COUNT(DISTINCT td.declaration_id) AS number_of_declarations, COALESCE(SUM(DISTINCT ta.assessed_amount), 0) AS total_assessed_amount, COALESCE(SUM(tpmt.payment_amount), 0) AS total_payment_amount FROM property pr INNER JOIN taxpayer tp ON pr.taxpayer_id = tp.taxpayer_id LEFT JOIN tax_registration tr ON tp.taxpayer_id = tr.taxpayer_id LEFT JOIN tax_declaration td ON tr.registration_id = td.registration_id LEFT JOIN tax_assessment ta ON td.declaration_id = ta.declaration_id LEFT JOIN tax_payment tpmt ON ta.assessment_id = tpmt.assessment_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, pr.property_location, pr.property_value HAVING COALESCE(SUM(tpmt.payment_amount), 0) < COALESCE(SUM(DISTINCT ta.assessed_amount), 0);


6.SELECT tp.taxpayer_tin, tp.taxpayer_name, v.plate_number, v.vehicle_value, tt.tax_type_name, COUNT(DISTINCT td.declaration_id) AS number_of_declarations, COALESCE(SUM(DISTINCT td.declared_amount), 0) AS total_declared_amount, COALESCE(SUM(DISTINCT ta.assessed_amount), 0) AS total_assessed_amount, COALESCE(SUM(p.penalty_amount), 0) AS total_penalties FROM taxpayer tp RIGHT JOIN vehicle v ON tp.taxpayer_id = v.taxpayer_id LEFT JOIN tax_registration tr ON v.taxpayer_id = tr.taxpayer_id LEFT JOIN tax_type tt ON tr.tax_type_id = tt.tax_type_id LEFT JOIN tax_declaration td ON tr.registration_id = td.registration_id LEFT JOIN tax_assessment ta ON td.declaration_id = ta.declaration_id LEFT JOIN penalty p ON ta.assessment_id = p.assessment_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, v.plate_number, v.vehicle_value, tt.tax_type_name HAVING v.vehicle_value > 10000000;

7.SELECT tp.taxpayer_tin, tp.taxpayer_name, tt.tax_type_name, tpr.period_start_date, tpr.period_end_date, tpr.filing_due_date, COUNT(DISTINCT td.declaration_id) AS number_of_declarations, COALESCE(SUM(DISTINCT td.declared_amount), 0) AS total_declared_amount, COALESCE(SUM(DISTINCT ta.assessed_amount), 0) AS total_assessed_amount, COALESCE(SUM(tpay.payment_amount), 0) AS total_amount_paid, COALESCE(SUM(DISTINCT ta.assessed_amount), 0) - COALESCE(SUM(tpay.payment_amount), 0) AS outstanding_balance FROM taxpayer tp INNER JOIN tax_registration tr ON tp.taxpayer_id = tr.taxpayer_id INNER JOIN tax_type tt ON tr.tax_type_id = tt.tax_type_id INNER JOIN tax_declaration td ON tr.registration_id = td.registration_id INNER JOIN tax_period tpr ON td.tax_period_id = tpr.tax_period_id INNER JOIN tax_assessment ta ON td.declaration_id = ta.declaration_id INNER JOIN tax_payment tpay ON ta.assessment_id = tpay.assessment_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, tt.tax_type_name, tpr.period_start_date, tpr.period_end_date, tpr.filing_due_date HAVING COALESCE(SUM(DISTINCT ta.assessed_amount), 0) - COALESCE(SUM(tpay.payment_amount), 0) > 0;


8.SELECT tp.taxpayer_tin, tp.taxpayer_name, ta.audit_status, tof.officer_name, tc.centre_name, tt.tax_type_name, COUNT(af.finding_id) AS number_of_audit_findings, COALESCE(SUM(af.finding_amount), 0) AS total_finding_amount FROM taxpayer tp INNER JOIN tax_audit ta ON tp.taxpayer_id = ta.taxpayer_id INNER JOIN tax_officer tof ON ta.officer_id = tof.officer_id INNER JOIN tax_centre tc ON tof.tax_centre_id = tc.tax_centre_id LEFT JOIN audit_finding af ON ta.audit_id = af.audit_id LEFT JOIN tax_type tt ON af.tax_type_id = tt.tax_type_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, ta.audit_status, tof.officer_name, tc.centre_name, tt.tax_type_name HAVING COALESCE(SUM(af.finding_amount), 0) > 2000000;


9.SELECT tof.officer_id, tof.officer_name, tof.officer_position, tc.centre_name, tc.district_name, COUNT(DISTINCT ta.audit_id) AS number_of_audits, COALESCE(SUM(af.finding_amount), 0) AS total_audit_finding_amount, COALESCE(AVG(af.finding_amount), 0) AS average_audit_finding_amount FROM tax_audit ta RIGHT JOIN tax_officer tof ON ta.officer_id = tof.officer_id INNER JOIN tax_centre tc ON tof.tax_centre_id = tc.tax_centre_id LEFT JOIN audit_finding af ON ta.audit_id = af.audit_id GROUP BY tof.officer_id, tof.officer_name, tof.officer_position, tc.centre_name, tc.district_name HAVING COALESCE(AVG(af.finding_amount), 0) > 500000;

10.SELECT tp.taxpayer_tin, tp.taxpayer_name, ta.assessment_id, ta.assessment_date, ta.assessed_amount, ob.objection_status, COALESCE(SUM(DISTINCT tpay.payment_amount), 0) AS total_payment_amount, COALESCE(SUM(DISTINCT p.penalty_amount), 0) AS total_penalty_amount FROM tax_assessment ta INNER JOIN tax_declaration td ON ta.declaration_id = td.declaration_id INNER JOIN tax_registration tr ON td.registration_id = tr.registration_id INNER JOIN taxpayer tp ON tr.taxpayer_id = tp.taxpayer_id INNER JOIN tax_objection ob ON ta.assessment_id = ob.assessment_id LEFT JOIN tax_payment tpay ON ta.assessment_id = tpay.assessment_id LEFT JOIN penalty p ON ta.assessment_id = p.assessment_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, ta.assessment_id, ta.assessment_date, ta.assessed_amount, ob.objection_status HAVING COUNT(ob.objection_id) >= 1 AND COALESCE(SUM(DISTINCT p.penalty_amount), 0) > 100000;




11.SELECT tp.taxpayer_tin, tp.taxpayer_name, ta.assessment_id, ta.assessed_amount, COUNT(DISTINCT ob.objection_id) AS number_of_objections, COALESCE(SUM(DISTINCT tpay.payment_amount), 0) AS total_payment_amount, ta.assessed_amount - COALESCE(SUM(DISTINCT tpay.payment_amount), 0) AS outstanding_balance FROM tax_assessment ta INNER JOIN tax_declaration td ON ta.declaration_id = td.declaration_id INNER JOIN tax_registration tr ON td.registration_id = tr.registration_id INNER JOIN taxpayer tp ON tr.taxpayer_id = tp.taxpayer_id LEFT JOIN tax_objection ob ON ta.assessment_id = ob.assessment_id LEFT JOIN tax_payment tpay ON ta.assessment_id = tpay.assessment_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, ta.assessment_id, ta.assessed_amount HAVING ta.assessed_amount - COALESCE(SUM(DISTINCT tpay.payment_amount), 0) > 500000;

12.SELECT b.bank_id, b.bank_name, b.bank_code, b.branch_name, COUNT(tp.payment_id) AS number_of_payments, COALESCE(SUM(tp.payment_amount), 0) AS total_payment_amount, COALESCE(AVG(tp.payment_amount), 0) AS average_payment_amount, COALESCE(MAX(tp.payment_amount), 0) AS maximum_payment_amount, COALESCE(MIN(tp.payment_amount), 0) AS minimum_payment_amount FROM tax_payment tp RIGHT JOIN bank b ON tp.bank_id = b.bank_id GROUP BY b.bank_id, b.bank_name, b.bank_code, b.branch_name HAVING COALESCE(SUM(tp.payment_amount), 0) < 20000000;


13.SELECT tp.taxpayer_tin, tp.taxpayer_name, tpay.payment_date, b.bank_name, tt.tax_type_name, COUNT(DISTINCT tpay.payment_id) AS number_of_payments, COALESCE(SUM(DISTINCT tpay.payment_amount), 0) AS total_payment_amount, COALESCE(SUM(trf.refund_amount), 0) AS total_refund_amount, COALESCE(SUM(DISTINCT tpay.payment_amount), 0) - COALESCE(SUM(trf.refund_amount), 0) AS net_revenue_collected FROM tax_payment tpay INNER JOIN bank b ON tpay.bank_id = b.bank_id INNER JOIN tax_assessment ta ON tpay.assessment_id = ta.assessment_id INNER JOIN tax_declaration td ON ta.declaration_id = td.declaration_id INNER JOIN tax_registration tr ON td.registration_id = tr.registration_id INNER JOIN taxpayer tp ON tr.taxpayer_id = tp.taxpayer_id INNER JOIN tax_type tt ON tr.tax_type_id = tt.tax_type_id LEFT JOIN tax_refund trf ON tpay.payment_id = trf.payment_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, tpay.payment_date, b.bank_name, tt.tax_type_name HAVING COALESCE(SUM(DISTINCT tpay.payment_amount), 0) - COALESCE(SUM(trf.refund_amount), 0) > 1000000;


14.SELECT tp.taxpayer_tin, tp.taxpayer_name, tpay.payment_id, tpay.payment_amount, COALESCE(SUM(trf.refund_amount), 0) AS refund_amount, MAX(trf.refund_date) AS refund_date, ROUND((COALESCE(SUM(trf.refund_amount), 0) / tpay.payment_amount) * 100, 2) AS refund_percentage FROM tax_payment tpay INNER JOIN tax_assessment ta ON tpay.assessment_id = ta.assessment_id INNER JOIN tax_declaration td ON ta.declaration_id = td.declaration_id INNER JOIN tax_registration tr ON td.registration_id = tr.registration_id INNER JOIN taxpayer tp ON tr.taxpayer_id = tp.taxpayer_id LEFT JOIN tax_refund trf ON tpay.payment_id = trf.payment_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, tpay.payment_id, tpay.payment_amount HAVING (COALESCE(SUM(trf.refund_amount), 0) / tpay.payment_amount) * 100 > 10;

15.SELECT tc.tax_centre_id, tc.centre_name, tc.district_name, tc.centre_manager, tt.tax_type_name, rt.target_year, rt.target_amount, COALESCE(SUM(DISTINCT td.declared_amount), 0) AS total_declared_amount, COALESCE(SUM(DISTINCT ta.assessed_amount), 0) AS total_assessed_amount, COALESCE(SUM(tpay.payment_amount), 0) AS total_revenue_collected FROM tax_registration tr RIGHT JOIN revenue_target rt ON tr.tax_centre_id = rt.tax_centre_id AND tr.tax_type_id = rt.tax_type_id INNER JOIN tax_centre tc ON rt.tax_centre_id = tc.tax_centre_id INNER JOIN tax_type tt ON rt.tax_type_id = tt.tax_type_id LEFT JOIN tax_declaration td ON tr.registration_id = td.registration_id LEFT JOIN tax_assessment ta ON td.declaration_id = ta.declaration_id LEFT JOIN tax_payment tpay ON ta.assessment_id = tpay.assessment_id GROUP BY tc.tax_centre_id, tc.centre_name, tc.district_name, tc.centre_manager, tt.tax_type_name, rt.target_year, rt.target_amount HAVING COALESCE(SUM(tpay.payment_amount), 0) < rt.target_amount;

16.SELECT tof.officer_id, tof.officer_name, tof.officer_position, tc.centre_name, COUNT(DISTINCT ta.assessment_id) AS number_of_assessments, COALESCE(SUM(DISTINCT ta.assessed_amount), 0) AS total_assessed_amount, COUNT(DISTINCT aud.audit_id) AS number_of_audits, COALESCE(SUM(DISTINCT af.finding_amount), 0) AS total_audit_finding_amount, COUNT(DISTINCT ec.enforcement_id) AS number_of_enforcement_cases, COALESCE(SUM(DISTINCT ec.outstanding_amount), 0) AS total_enforcement_outstanding_amount FROM tax_officer tof INNER JOIN tax_centre tc ON tof.tax_centre_id = tc.tax_centre_id LEFT JOIN tax_assessment ta ON tof.officer_id = ta.officer_id LEFT JOIN tax_audit aud ON tof.officer_id = aud.officer_id LEFT JOIN audit_finding af ON aud.audit_id = af.audit_id LEFT JOIN enforcement_case ec ON tof.officer_id = ec.officer_id GROUP BY tof.officer_id, tof.officer_name, tof.officer_position, tc.centre_name HAVING COUNT(DISTINCT ta.assessment_id) > 5 AND COALESCE(SUM(DISTINCT ec.outstanding_amount), 0) > 1000000;

17.SELECT tp.taxpayer_tin, tp.taxpayer_name, COUNT(DISTINCT b.business_id) AS number_of_businesses, COUNT(DISTINCT pr.property_id) AS number_of_properties, COUNT(DISTINCT v.vehicle_id) AS number_of_vehicles, COALESCE(SUM(DISTINCT pr.property_value), 0) AS total_property_value, COALESCE(SUM(DISTINCT v.vehicle_value), 0) AS total_vehicle_value, COUNT(DISTINCT td.declaration_id) AS number_of_declarations, COALESCE(SUM(DISTINCT ta.assessed_amount), 0) AS total_assessed_amount, COALESCE(SUM(DISTINCT tpay.payment_amount), 0) AS total_payment_amount, COALESCE(SUM(DISTINCT trf.refund_amount), 0) AS total_refund_amount FROM taxpayer tp LEFT JOIN business b ON tp.taxpayer_id = b.taxpayer_id LEFT JOIN property pr ON tp.taxpayer_id = pr.taxpayer_id LEFT JOIN vehicle v ON tp.taxpayer_id = v.taxpayer_id LEFT JOIN tax_registration tr ON tp.taxpayer_id = tr.taxpayer_id LEFT JOIN tax_declaration td ON tr.registration_id = td.registration_id LEFT JOIN tax_assessment ta ON td.declaration_id = ta.declaration_id LEFT JOIN tax_payment tpay ON ta.assessment_id = tpay.assessment_id LEFT JOIN tax_refund trf ON tpay.payment_id = trf.payment_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name HAVING COALESCE(SUM(DISTINCT pr.property_value), 0) + COALESCE(SUM(DISTINCT v.vehicle_value), 0) > 50000000;



18.SELECT tp.taxpayer_tin, tp.taxpayer_name, b.business_sector, COUNT(DISTINCT b.business_id) AS number_of_businesses, COUNT(DISTINCT pr.property_id) AS number_of_properties, COUNT(DISTINCT v.vehicle_id) AS number_of_vehicles, COALESCE(SUM(DISTINCT td.declared_amount), 0) AS total_declared_amount, COALESCE(SUM(DISTINCT ta.assessed_amount), 0) AS total_assessed_amount, COALESCE(SUM(DISTINCT tpay.payment_amount), 0) AS total_payment_amount, COALESCE(SUM(DISTINCT p.penalty_amount), 0) AS total_penalty_amount, COALESCE(SUM(DISTINCT af.finding_amount), 0) AS total_audit_finding_amount FROM taxpayer tp RIGHT JOIN business b ON tp.taxpayer_id = b.taxpayer_id LEFT JOIN property pr ON b.taxpayer_id = pr.taxpayer_id LEFT JOIN vehicle v ON b.taxpayer_id = v.taxpayer_id LEFT JOIN tax_registration tr ON b.taxpayer_id = tr.taxpayer_id LEFT JOIN tax_declaration td ON tr.registration_id = td.registration_id LEFT JOIN tax_assessment ta ON td.declaration_id = ta.declaration_id LEFT JOIN tax_payment tpay ON ta.assessment_id = tpay.assessment_id LEFT JOIN penalty p ON ta.assessment_id = p.assessment_id LEFT JOIN tax_audit aud ON b.taxpayer_id = aud.taxpayer_id LEFT JOIN audit_finding af ON aud.audit_id = af.audit_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, b.business_sector HAVING COUNT(DISTINCT pr.property_id) + COUNT(DISTINCT v.vehicle_id) > 1;


19.SELECT tp.taxpayer_tin, tp.taxpayer_name, tt.tax_type_name, tc.centre_name, tpr.period_start_date, tpr.period_end_date, tpr.filing_due_date, COUNT(DISTINCT td.declaration_id) FILTER (WHERE td.declaration_date > tpr.filing_due_date) AS number_of_late_declarations, COALESCE(SUM(DISTINCT td.declared_amount), 0) AS total_declared_amount, COALESCE(SUM(DISTINCT ta.assessed_amount), 0) AS total_assessed_amount, COALESCE(SUM(DISTINCT p.penalty_amount), 0) AS total_penalty_amount, COALESCE(SUM(DISTINCT tpay.payment_amount), 0) AS total_payment_amount FROM taxpayer tp INNER JOIN tax_registration tr ON tp.taxpayer_id = tr.taxpayer_id INNER JOIN tax_type tt ON tr.tax_type_id = tt.tax_type_id INNER JOIN tax_centre tc ON tr.tax_centre_id = tc.tax_centre_id INNER JOIN tax_declaration td ON tr.registration_id = td.registration_id INNER JOIN tax_period tpr ON td.tax_period_id = tpr.tax_period_id INNER JOIN tax_assessment ta ON td.declaration_id = ta.declaration_id LEFT JOIN penalty p ON ta.assessment_id = p.assessment_id LEFT JOIN tax_payment tpay ON ta.assessment_id = tpay.assessment_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, tt.tax_type_name, tc.centre_name, tpr.period_start_date, tpr.period_end_date, tpr.filing_due_date HAVING COUNT(DISTINCT td.declaration_id) FILTER (WHERE td.declaration_date > tpr.filing_due_date) > 2;


20.SELECT tp.taxpayer_tin, tp.taxpayer_name, tp.taxpayer_type, b.business_name, pr.property_location, v.plate_number, tt.tax_type_name, tt.filing_frequency, tc.centre_name, tc.district_name, tof.officer_name, bk.bank_name, COUNT(DISTINCT td.declaration_id) AS number_of_declarations, COALESCE(SUM(DISTINCT td.declared_amount), 0) AS total_declared_amount, COALESCE(SUM(DISTINCT ta.assessed_amount), 0) AS total_assessed_amount, COALESCE(SUM(DISTINCT tpay.payment_amount), 0) AS total_payment_amount, COALESCE(SUM(DISTINCT p.penalty_amount), 0) AS total_penalty_amount, COALESCE(SUM(DISTINCT af.finding_amount), 0) AS total_audit_finding_amount, COALESCE(SUM(DISTINCT trf.refund_amount), 0) AS total_refund_amount, COALESCE(SUM(DISTINCT ec.outstanding_amount), 0) AS total_enforcement_outstanding_amount, rt.target_amount, ROUND((COALESCE(SUM(DISTINCT tpay.payment_amount), 0) / rt.target_amount) * 100, 2) AS revenue_performance_percentage FROM taxpayer tp INNER JOIN tax_registration tr ON tp.taxpayer_id = tr.taxpayer_id INNER JOIN tax_type tt ON tr.tax_type_id = tt.tax_type_id INNER JOIN tax_centre tc ON tr.tax_centre_id = tc.tax_centre_id INNER JOIN tax_declaration td ON tr.registration_id = td.registration_id INNER JOIN tax_assessment ta ON td.declaration_id = ta.declaration_id INNER JOIN tax_officer tof ON ta.officer_id = tof.officer_id LEFT JOIN tax_payment tpay ON ta.assessment_id = tpay.assessment_id LEFT JOIN bank bk ON tpay.bank_id = bk.bank_id LEFT JOIN tax_refund trf ON tpay.payment_id = trf.payment_id LEFT JOIN penalty p ON ta.assessment_id = p.assessment_id LEFT JOIN business b ON tp.taxpayer_id = b.taxpayer_id LEFT JOIN property pr ON tp.taxpayer_id = pr.taxpayer_id LEFT JOIN vehicle v ON tp.taxpayer_id = v.taxpayer_id LEFT JOIN tax_audit aud ON tp.taxpayer_id = aud.taxpayer_id LEFT JOIN audit_finding af ON aud.audit_id = af.audit_id LEFT JOIN enforcement_case ec ON tp.taxpayer_id = ec.taxpayer_id RIGHT JOIN revenue_target rt ON tr.tax_centre_id = rt.tax_centre_id AND tr.tax_type_id = rt.tax_type_id GROUP BY tp.taxpayer_tin, tp.taxpayer_name, tp.taxpayer_type, b.business_name, pr.property_location, v.plate_number, tt.tax_type_name, tt.filing_frequency, tc.centre_name, tc.district_name, tof.officer_name, bk.bank_name, rt.target_amount HAVING COALESCE(SUM(DISTINCT ta.assessed_amount), 0) > COALESCE(SUM(DISTINCT td.declared_amount), 0) AND COALESCE(SUM(DISTINCT tpay.payment_amount), 0) > 0 AND COALESCE(SUM(DISTINCT ec.outstanding_amount), 0) > 0;

